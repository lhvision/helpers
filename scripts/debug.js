/* eslint-disable antfu/no-import-dist */
import { } from '../dist/index.js'
import { } from '../dist/upload.js'

// const testFile = new File(['test file content test file content test file content test file content'], 'test.txt', { type: 'text/plain' })

// const mockHandlers = {
//   uploadChunk: async (chunk) => {
//     if (chunk.index === 2) {
//       throw new Error('Upload failed')
//     }
//     return { success: true }
//   },
//   mergeChunks: (filename, chunks) => Promise.resolve({ success: true }),
// }

// uploadFileInChunks(testFile, mockHandlers, {
//   concurrency: 10,
//   chunkSize: 1,
//   exitOnError: true,
// }).catch((error) => {
//   console.log('error', error)
// })
